server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog?useSSL=false&characterEncoding=utf-8
    username: root
    password: Wd.020518
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    # SMTP服务器地址
    host: smtp.163.com
    # SMTP端口
    port: 465
    # 邮箱用户名
    username: WindyIslandBlog@163.com
    # 邮箱授权码
    password: QFZWVQFDAXLFQFFD
    # 邮件编码
    default-encoding: UTF-8


logging:
  level:
    root: info
